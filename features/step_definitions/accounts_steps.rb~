Given /User is logged in as (.*)/ do |email|
  :user_id=>User.find_by_email(email)

end
Given /the following accounts exist:/ do |accounts_table|
  accounts_table.hashes.each do |account|
    Account.create!(:user_id,account)
  end
end
